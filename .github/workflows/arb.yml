name: Update arbitrage tracker

on:
  schedule:
    # Every 30 minutes. Arbitrage windows are short-lived; the daily
    # update.yml cadence is far too slow for this page.
    - cron: "*/30 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: update-arb
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run engine tests
        run: python -m unittest test_arb_engine test_arb_agent

      - name: Build arbitrage page
        env:
          # Agent B: the market brief. Absent key = the brief section is
          # simply omitted; the rest of the page builds as before.
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_MODEL: "claude-sonnet-5"
        run: python build_arb.py

      - name: Commit updated page + data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # Explicit paths: a new output file is silently never committed
          # unless it is listed here.
          git add docs/arb.html data/arb_current.csv data/arb_log.csv data/arb_brief.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Arb tracker update ($(date -u +%Y-%m-%dT%H:%M))"
            # The daily dashboard workflow may push concurrently; rebase first.
            git pull --rebase
            git push
          fi
